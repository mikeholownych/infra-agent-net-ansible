version: "3.8"
services:
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: ai
      POSTGRES_PASSWORD: ai
      POSTGRES_DB: ai_memory
    volumes:
      - db_data:/var/lib/postgresql/data
volumes:
  db_data:
